# MWDMonitoringReceiver

Receiver service installation is pending.

Do not install a separate receiver service until a standalone receiver/gateway entrypoint is verified. Current gateway/receiver behavior is started by the backend process.
